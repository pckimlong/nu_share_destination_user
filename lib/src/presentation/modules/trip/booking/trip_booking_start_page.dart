import 'package:flutter/material.dart';
import 'package:hooks_riverpod/hooks_riverpod.dart';

class TripBookingStartPage extends ConsumerWidget {
  /// When user already place a booking. This widget show countdown
  /// and driver who accepted
  const TripBookingStartPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context, WidgetRef ref) {
    return Container();
  }
}
