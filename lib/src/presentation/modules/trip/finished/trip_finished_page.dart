import 'package:flutter/material.dart';
import 'package:hooks_riverpod/hooks_riverpod.dart';

class TripFinishedPage extends ConsumerWidget {
  /// After trip finished in any passenger. Show infomation and receipt...
  const TripFinishedPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context, WidgetRef ref) {
    return Container();
  }
}
